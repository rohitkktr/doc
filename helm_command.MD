helm  repo add helm <url> --username <user name> --password <token/password>
genrate templet using command eg : `helm template <chart> -f <values file > 
to genrate new version use update Chart.yaml : `helm\Chart.yaml`
after verify changes command to create package : `helm package . --destination ../packages`
update helm index using command  `helm repo index --merge index.yaml ./`
